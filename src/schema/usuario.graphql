type Perfil {
  id: Int
  nome: String
}

enum UsuarioStatus {
  ATIVO
  INATIVO
  BLOQUEADO
}

type Usuario {
  id: Int
  nome: String!
  email: String!
  ano_nasc: Int
  perfil: Perfil
  salario: Float
  status: UsuarioStatus
}
